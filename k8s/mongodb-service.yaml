apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: mern-ecommerce
  labels:
    app: mongodb
    tier: database
spec:
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb
  type: ClusterIP